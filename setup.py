from setuptools import setup

setup(
    name='qancode',
    version='1.0',
    description='A useful module',
    author='Man Foo',
    author_email='foomail@foo.com',
    packages=['qancode'],  #same as name
    install_requires=[], #external packages as dependencies
)
